using System;

class Program
{
    static void Main(string[] args)
    {
        //I didn't add much extra. I experimented with using both Lists and arrays. I also started using
        //the 'protected' type of variables.
        Goals goalList = new Goals();
        goalList.MainMenu();
    }
}